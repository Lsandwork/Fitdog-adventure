# Fitdog avatar asset

## Required file

Place the **exact uploaded** Fitdog avatar PNG here:

- `public/assets/avatar/fitdog-avatar.png` → served on web as `/assets/avatar/fitdog-avatar.png`
- `assets/avatar/fitdog-avatar.png` → bundled for native via `require`

Both paths must be the **same original file**, byte-for-byte.

## Rules

- Do not redraw, recreate, vectorize, crop, recolor, or replace with SVG/emoji.
- Preserve transparency and aspect ratio.
- UI uses `resizeMode="contain"` / `object-fit: contain` only.

## Current placeholder

If these files are a tiny transparent stub, **overwrite them** with your uploaded `fitdog-avatar.png`.
